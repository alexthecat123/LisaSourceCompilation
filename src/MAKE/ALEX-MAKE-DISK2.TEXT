$EXEC{Optionally formats LOS install disk 2 and copies the installer files to it}$WRITE "Insert a diskette for LOS Install Disk 2 into the drive and press return..."$READLN %0$WRITE "Do you want to format the disk (Y/N)? [N] "$READLN %1$DOITF{ilemgr}$IF UPPERCASE(%1) = 'Y' THEN    I{nitialize}lower    $IF EXISTS('-lower') THEN {If we want to init and its already an LOS disk}        Y{es, destroy current directory}{Format full size of disk}    $ELSE        {Format fill size of disk}    $ENDIF    Office System 2 3.0 {Disk name}    25 {Max number of files on disk}$ENDIFR{ename}-lower-Office System 2 3.0 {Rename, in case we did not init earlier}D{elete}-lower-= {Kill all files on disk, in case we did not format it}Y{es, delete all}D{elete}-lower-~{=Y{es, delete all hidden files}{Now copy over all our files}C{opy}IOSFPLIB.OBJ {WE HAVENT BUILT THIS ONE YET B/C OF MISSING FILE, USING OG FOR NOW}-LOWER-IOSFPLIB.OBJC{opy}APLC/LCORBGLIB.OBJ-LOWER-LCORBGLIB.OBJC{opy}OBJIOLIB.OBJ {I DONT THINK WE HAVE WHAT WE NEED TO BUILD THIS, USING OG FOR NOW}-LOWER-OBJIOLIB.OBJC{opy}LIBPR/PRLIB.OBJ-LOWER-PRLIB.OBJC{opy}LIBS/SYS1LIB.OBJ-LOWER-SYS1LIB.OBJC{opy}LIBPR/SYSTEM.PR_DWP.OBJSYSTEM.PR_Daisy Wheel PrinterU{nmount}lowerQ{uit}$ENDEXEC