; Alex - This was not in the original source release, so I had to add it; by disassembling the original IOSPASLIB and copying the code from there..DEF    %I_PWRII%I_PWRII:    LINK     A6,#$0000    MOVEM.L  D0-D2,-(A7)    MOVEQ    #$01,D2    MOVE.L   $0014(A7),D0    MOVE.L   $0018(A7),D1    BPL.S    @1    NEG.L    D1@1  BEQ.S    @5@2  BTST     #$0000,D1    BEQ.S    @3    MOVE.L   D0,-(A7)    MOVE.L   D2,-(A7)    JSR      %I_MUL4    MOVE.L   (A7)+,D2@3  CMPI.L   #$00000001,D1    BLE.S    @4    MOVE.L   D0,-(A7)    MOVE.L   D0,-(A7)    JSR      %I_MUL4    MOVE.L   (A7)+,D0@4  ASR.L    #$1,D1    BNE.S    @2@5  MOVE.L   $0018(A7),D1    BGE.S    @6    MOVEQ    #$01,D0    MOVE.L   D0,-(A7)    MOVE.L   D2,-(A7)    JSR      %I_DIV4    MOVE.L   (A7)+,D2@6  MOVE.L   D2,$0018(A7)    MOVEM.L  (A7)+,D0-D2    UNLK     A6    MOVE.L   (A7),$0004(A7)    ADDQ.L   #$4,A7    RTS    .END